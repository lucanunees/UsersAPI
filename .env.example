# Copie este arquivo para ".env" e preencha com valores reais.
# O docker-compose le automaticamente o arquivo ".env" na raiz do projeto.

# MongoDB Atlas (perfil estendido do usuario)
MONGO_CONNECTION_STRING=mongodb+srv://<usuario>:<senha>@<cluster>.mongodb.net/?appName=ClusterFiap
MONGO_DB=users
