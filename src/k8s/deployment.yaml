apiVersion: apps/v1
kind: Deployment
metadata:
  name: users-api
  namespace: fcg-tech-fase-4
  labels:
    app: users-api
    version: v1
    component: api
spec:
  replicas: 2
  
  selector:
    matchLabels:
      app: users-api
  
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  
  template:
    metadata:
      labels:
        app: users-api
        version: v1
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "80"
        prometheus.io/path: "/metrics"
    
    spec:
      # Security Context
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      
      containers:
      - name: users-api
        image: 159924930744.dkr.ecr.us-east-1.amazonaws.com/usersapi:v6
        imagePullPolicy: Always
        
        ports:
        - containerPort: 80
          name: http
          protocol: TCP
        
        # ========================================
        # Variáveis de Ambiente
        # ========================================
        env:
        - name: Swagger__Enabled
          value: "true"
        # ASP.NET Core
        - name: ASPNETCORE_ENVIRONMENT
          value: Production
        - name: ASPNETCORE_URLS
          value: http://+:80
        
        # Database Connection String
        - name: ConnectionStrings__DefaultConnection
          valueFrom:
            secretKeyRef:
              name: users-secret
              key: connection-string
        - name: MONGO_DB
          value: users

        - name: MONGO_CONNECTION_STRING
          valueFrom:
            secretKeyRef:
              name: users-mongodb-secret
              key: connection-string
        
        - name: RabbitMq__Username
          valueFrom:
            secretKeyRef:
              name: rabbitmq-secret
              key: username

        - name: RabbitMq__Password
          valueFrom:
            secretKeyRef:
              name: rabbitmq-secret
              key: password
        
        # JWT Configuration
        - name: Jwt__Issuer
          valueFrom:
            configMapKeyRef:
              name: users-config
              key: jwt-issuer
        - name: Jwt__Audience
          valueFrom:
            configMapKeyRef:
              name: users-config
              key: jwt-audience
        - name: Jwt__Key
          valueFrom:
            secretKeyRef:
              name: users-secret
              key: jwt-key
        - name: Jwt__ExpirationInMinutes
          valueFrom:
            configMapKeyRef:
              name: users-config
              key: jwt-expiration
        
        # RabbitMQ
        - name: RabbitMq__Host
          value: rabbitmq
        
        # Redis Cache
        - name: REDIS_HOST
          value: redis
        - name: REDIS_PORT
          value: "6379"
        - name: REDIS_PASSWORD
          valueFrom:
            secretKeyRef:
              name: redis-secret
              key: password
        
        # ========================================
        # Resources
        # ========================================
        resources:
          requests:
            memory: "256Mi"
            cpu: "200m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        
        # ========================================
        # Health Checks
        # ========================================
        livenessProbe:
          httpGet:
            path: /health
            port: 80
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        
        readinessProbe:
          httpGet:
            path: /health/ready
            port: 80
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        
        # ========================================
        # Startup Probe (para inicialização lenta)
        # ========================================
        startupProbe:
          httpGet:
            path: /health
            port: 80
          initialDelaySeconds: 0
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 30
      
      # ========================================
      # Restart Policy
      # ========================================
      restartPolicy: Always
